import { IMinecraftData } from './interfaces';
/**
 * ping with URI
 * @param uri minecraft://server[:port]
 */
export declare const pingUri: (uri: string) => Promise<IMinecraftData>;
/**
 * ping with hostname and port
 * @param hostname hostname
 * @param port port number (defaults 25565)
 */
export declare const ping: (hostname?: string, port?: number) => Promise<IMinecraftData>;
